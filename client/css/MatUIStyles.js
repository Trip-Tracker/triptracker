import AppBar from '@material-ui/core/AppBar';
import { styled } from '@material-ui/core/styles';

export const TTNavBar = styled(AppBar)({
  color: '#ffffff',
  backgroundColor: '#720000',
  position: 'relative',
});